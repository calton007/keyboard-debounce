# Changelog

## Unreleased

- 修复 WinUI 设置窗口未应用 EXE 内嵌图标、标题栏显示通用占位图标的问题。
- 收紧自学习：放行事件不再上调；只有物理释放后的连续贴边拦截才按观测节奏换算目标阈值，并限制在默认值上方 `40ms` 内；稳定输入按 12 次一档回落且不低于默认值。
- 学习状态升级到 Schema v2；首次加载旧数据时只重置高于默认值的阈值，保留较低阈值与统计，并创建永不覆盖的 `learning-state.pre-v2.json` 后按原子保存和指数退避写回。
- 游戏检测、托盘、概览和游戏页改用同一份不可变运行态快照；一次检测返回全部匹配 EXE，状态变化推进 UI 运行态版本，当前页事件刷新、隐藏页激活补刷。
- 游戏检测部分或整体失败时保留上次实际模式并明确显示未知及原始错误；关闭开关、清空名单或移除当前命中项仍立即恢复普通模式。
- 概览页按实际模式切换卡片高亮并补齐游戏阈值、长按、游戏防抖键数和活动 EXE；EXE 后缀统一只在展示层添加一次。
- 将设置窗口完成 WinUI 3 重构：五页顶部导航、右上全局开关、概览状态摘要和 Scheme 3 固定浅色方案。
- 新增按键名称/VK 搜索和“全部、仅已学习、仅游戏防抖、仅始终忽略”筛选，补齐分页切换与刷新场景下筛选、排序、选中项和输入焦点保持。
- 五页在窗口创建时完成一次预热，之后复用缓存实例并保留筛选、排序和当前焦点，消除首次切页卡顿。
- 启用 Per-Monitor V2 DPI 与 WinUI 3 键盘焦点样式，修复 150% 等场景的标题、输入控件和页面宽度异常。
- 将游戏模式页重构为双栏控制中心：左侧集中状态与可同步调节参数，右侧集中游戏 EXE 管理和逐行运行状态。
- 移除手工进程名输入，新增“浏览选择 EXE”和“从正在运行的程序添加”两个批量入口；首次添加自动开启模式，旧配置字段保持兼容。
- 删除五页主标题下的页面级副标题，仅保留功能卡片和控件内说明文案，收敛界面层级与视觉噪音。
- 按键管理页改为自适应高度布局：列表卡片不再固定高度，随可用区域扩展，确保 1160×760 下至少 5 行、960×640 下至少 3 行可见，便于批量查看。
- 运行中 EXE 枚举改为稳定排序、去重并排除当前应用；多个配置项同时命中时按配置顺序稳定显示。
- 将设置页和运行中 EXE 选择器的进程枚举移出 UI 线程；读取失败时明确显示未知状态，并在后续成功读取前保留错误。
- 修复后台检测期间关闭游戏模式或移除命中 EXE 时旧模式可能残留的问题；配置变更会立即失活并排队重新检测。
- 动态状态、错误和选择数量会同步暴露给 Windows UI Automation；浅色主题下的运行状态颜色满足正文对比度要求。
- 修复 WinUI 3 自包含单文件发布的运行时基目录初始化与重复资产声明，确保正式 EXE 可直接冷启动。

## 0.2.0 - 2026-08-09

- 新增用户维护的进程游戏模式：指定进程运行即启用，进程退出即恢复；不自动识别游戏。
- 游戏模式支持独立阈值上限、长按时间和故障键名单，并完全冻结普通模式学习数据。
- 运行态使用扫描码和 Extended 标志区分物理键，修复同虚拟键码之间的时间线干扰。
- 暂停热键支持界面重绑和失败保留，开机自启会校验当前可执行文件路径并正确回滚失败状态。
- 设置界面补齐游戏进程、游戏防抖键、启动延迟和热键管理，并显示当前匹配进程。
- 发布脚本改为完整暂存校验和三产物事务提交；目标被占用或提交中途失败时保留原发布物且不结束进程。
- 新增统一 `scripts\verify.ps1` 和 Windows CI，并扩充游戏模式、热键、自启、持久化和发布回滚回归测试。

## 0.1.1 - 2026-07-11

- 修正持续抖动周期漏放、KeyDown/KeyUp 不配对和计时器回绕问题。
- 修饰键与软件注入输入改为安全直通；暂停和启动延迟不再更新学习状态。
- 默认暂停热键迁移到 `Ctrl+Alt+F11`，注册失败时保持停用。
- 增加单实例保护、配置原子写入、备份恢复和损坏文件隔离。
- 学习统计改为限频后台检查点；保存失败会自动退避重试并持续显示错误。
- 发布改为带版本校验的 `win-x64` 自包含单文件，并生成 SHA256；ZIP 包含 LICENSE。

## 0.1.0 - 2026-06-07

- 整理为正式 `net8.0-windows` WinForms 源码项目。
- 保留自学习键盘防抖、忽略键、托盘控制、设置窗口和自定义图标。
- 将发布物统一放到 `releases/`，命名为 `KeyboardDebounce-0.1.0-win-x64.*`。
- 将原控制台断言测试迁移为 xUnit 测试项目。
